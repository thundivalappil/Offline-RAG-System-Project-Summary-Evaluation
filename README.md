# Offline-RAG-System-Project-Summary-Evaluation
This project implements a fully offline Retrieval-Augmented Generation (RAG) system using ChromaDB and Sentence Transformers (MiniLM). The system loads text documents, splits them into chunks, generates embeddings, stores vectors persistently, retrieves the most relevant context, and extracts a clean sentence-level answer with source citation.
